#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
/*	int a, b;
	while (1) { 
	printf("더할 두 수 입력 :")
	scanf("%d, %d", &a, &b);r
	
	printf("%d + %d = %d \n",a ,b, a+b); 
	}
}
*/
   /* int a, b;
	char ch;
	
	while (1){
		printf("계산할 두 수를 입력 :");
		scanf("%d %d",&a,&b);
		
		printf("계산할 연산자: ");
		scanf(" %c", &ch);
		
		switch(ch){
			case '+';
			printf("%d + %d = %d\n",a, b, a+b);
			break;
			case '-';
			printf("%d - %d = %d\n",a, b, a-b);
			break;
			case '*';
			printf("%d * %d = %d\n",a, b, a*b);
			break;
			case '/';
			printf("%d / %d = %d\n",a, b, a/(float)b);
			break;
			case '%';
			printf("%d %% %d = %d\n",a, b, a%b);
			break;
			default :
				printf("연산자 잘못입력");
		} 
	}	
	*/
	/*int a =100;
	
	while(a == 200){
	printf("while문 내부에 들어왔습니다.\n"); 
}
do{
printf("do ~ while 문 내부에 들어 왔습니다 \n");
}while (a==200);
}

/* while문으로 바꿔보기 
int menu;

do {
printf("\n 손님 주문하셈\n");
printf("<1>카페라떼 <2>카푸치노 <3>아메리카노 <4>그만 ==>");
scanf("%d",&menu);

switch(menu){

case1 : printf("#카페라떼 주문했슴.\n"); break;
case2 : printf("#카푸치노 주문했슴.\n"); break;
case3 : printf("#아메리카노 주문했슴.\n"); break;
case4 : printf("# 주문한거 준비함.\n"); break;
default : printf("잘못했슴\n");
}
}while(menu !=4);
}
*/
/*
int i;

for ( i =1; i<101; i++){
printf("for문을 %d회 실행함\n",i);
break;
}
printf("for문를 종료함\n")
}
*/

/*int hap = 0;
int i;

for (i=1; i< 101; i++){
hap =hap +i;
if(hap >1001){
break;
}
printf("1~100의 합에서 최초로 1000이 넘는 위치는? : %d \n",i);
}
*/
/*
int hap = 0;
int i;

for ( i=1; i < 101; i++){

if(i%3==0)
continue;
hap=+i;
}
printf("1 - 100 까지의 합 (3의 배수제외) : %d\n",hap);
}
*/

/* 
